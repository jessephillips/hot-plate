.container.container-sm
  .box
    %h2.text-center Edit #{resource_name.to_s.titleize}

    = simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f|
      = f.error_notification
      .form-inputs
        .form-group
          = f.input :username, required: true, input_html: { class: 'form-control' }, autofocus: true
        .form-group
          = f.input :email, required: true, input_html: { class: 'form-control' }
        - if devise_mapping.confirmable? && resource.pending_reconfirmation?
          %p
            Currently waiting confirmation for: #{resource.unconfirmed_email}
        .form-group
          = f.input :password, autocomplete: 'off', input_html: { class: 'form-control' }, hint: 'leave it blank if you don\'t want to change it', required: false
        .form-group
          = f.input :password_confirmation, input_html: { class: 'form-control' }, required: false
        .form-group
          = f.input :current_password, input_html: { class: 'form-control' }, hint: 'we need your current password to confirm your changes', required: true
      .form-actions
        = f.button :submit, 'Update', class: 'btn btn-primary btn-lg btn-block'
